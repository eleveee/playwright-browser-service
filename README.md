# Playwright Browser Service (FastAPI)

A minimal browser service using Playwright (Chromium) + FastAPI, ready for Zeabur.

## Endpoints

### GET /health
Returns service and browser status.

### POST /screenshot
Request: